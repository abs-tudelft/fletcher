image: docker:latest

stages:
  - check
  - test
  - build
  - examples

# before_script:
#   - echo $CI_REGISTRY_PASSWORD | docker login -u $CI_REGISTRY_USER $CI_REGISTRY --password-stdin

.env: &env
  ARROW_VERSION: 0.11.1

  GHDL_IMAGE: ghdl/ghdl
  GHDL_TAG: ubuntu18-llvm-5.0

  RUST_VERSION: 1.30.1
  RUST_VHDL_PARSER_CRATE_VERSION: 0.3.0

  FLETCHER_APT_PACKAGES: cmake g++

  # cmake
  FLETCHER_TESTS: 1

variables:
  <<: *env

.apt-install: &apt-install |
  [ "$APT_PACKAGES" ] && apt-get update && apt-get install -y $APT_PACKAGES

vhdl-syntax:
  stage: check
  image: rust:${RUST_VERSION}-slim-stretch
  before_script:
    - cargo install vhdl_parser --version $RUST_VHDL_PARSER_CRATE_VERSION
  script:
    - find hardware -name "*.vhd" |
      xargs vhdl_parser --show

.ghdl-check-job: &ghdl-check-job
  stage: check
  image: $GHDL_IMAGE:$GHDL_TAG
  script:
    - find hardware/vhdl -name "*.vhd" |
      xargs ghdl -i --std=${STD:-08} |
      grep entity |
      sed -e 's/entity //' |
      sed -e 's/ \*\*//' |
      xargs -L 1 ghdl -m --std=${STD:-08} --ieee=synopsys

vhdl-93c:
  <<: *ghdl-check-job
  variables:
    <<: *env
    STD: 93c

vhdl-08:
  <<: *ghdl-check-job

vhdl-tests:
  <<: *ghdl-check-job
  stage: test
  allow_failure: true
  script:
    - find hardware -name "*.vhd" |
      xargs ghdl -i --std=${STD:-08} |
      grep entity |
      grep _tb |
      sed -e 's/entity //' |
      sed -e 's/ \*\*//' |
      xargs -i -t bash -c '
      ghdl -m --std=${STD:-08} --ieee=synopsys {};
      ghdl -r --std=${STD:-08} --ieee=synopsys {} --stop-time=100ns'

.cmake-job: &cmake-job
  stage: test
  image: mbrobbel/libarrow:$ARROW_VERSION
  variables:
    <<: *env
    APT_PACKAGES: $FLETCHER_APT_PACKAGES
  before_script:
    - *apt-install
    - [ "$FLETCHER_TESTS" ] && apt-get install -y libgtest-dev
    - mkdir -p build
  script:
    - pushd build
    - cmake
        -DFLETCHER_TESTS=$FLETCHER_TESTS
        ../$CI_JOB_NAME
    - make -j
    - [ "$FLETCHER_TESTS" ] && make test

runtime/cpp:
  <<: *cmake-job

common/cpp:
  <<: *cmake-job
